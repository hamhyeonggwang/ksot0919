/**
 * VIP RSVP 설정
 * ✏️ 아래 두 값을 채운 뒤 저장하세요.
 *
 * [설정 방법]
 * 1. VIP_SCRIPT_URL:
 *    Apps Script 에디터 → 배포 → 웹 앱으로 배포 → URL 복사
 *    (기존 접수 시스템과 동일한 배포 URL 사용 가능)
 *
 * 2. VIP_SECRET:
 *    임의 문자열을 정하고 (예: "ksot-vip-2026")
 *    Apps Script 에디터 → 프로젝트 설정 → 스크립트 속성
 *    → VIP_SECRET 키에 동일한 값 입력
 */

const VIP_SCRIPT_URL = '';   // ✏️ Apps Script 배포 URL
const VIP_SECRET     = '';   // ✏️ Script Properties > VIP_SECRET 와 동일한 값
