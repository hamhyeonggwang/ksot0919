# (구) 하이브리드 설정 — 폐기

이 문서는 **relay-sync + Database Webhook** 방식으로, 오류 가능성이 있어 **폐기**되었습니다.

**현재 설정 가이드:** [`docs/SUBMIT-SETUP.md`](SUBMIT-SETUP.md)

- 단일 Edge Function `submit` (동기 처리)
- Webhook / relay-sync 없음
- 브라우저 → GAS 직접 호출 없음
